# ============================================================================
# SUPABASE CONFIGURATION (Required for backend)
# ============================================================================
# Get these values from your Supabase project settings at https://supabase.com
# Project Settings > API

# Your Supabase project URL
VITE_SUPABASE_URL=your_supabase_project_url

# Your Supabase anon/public key (safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Your Supabase service role key (NEVER expose in frontend, only for backend operations)
# This key bypasses Row Level Security - keep it secret!
VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================================================
# LLM PROVIDER CONFIGURATION
# ============================================================================
# Choose between 'base44' (legacy) or 'gemini'
# Note: Base44 LLM integration needs to be replaced with your own LLM provider
# after migration. See src/lib/custom-sdk.js for implementation details.
VITE_LLM_PROVIDER=gemini

# Gemini API Configuration
# Required if using VITE_LLM_PROVIDER=gemini
# Get your API key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================================
# APPLICATION CONFIGURATION (Optional)
# ============================================================================
VITE_APP_NAME=AEO Presence
VITE_APP_DOMAIN=http://localhost:5173
